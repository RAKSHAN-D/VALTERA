<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #d9f4e4, #f0fff5);
      color: #2c5f2d;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 40px;
      min-height: 100vh;
      justify-content: center;
    }

    h1 {
      color: #24532f;
      text-align: center;
      margin-bottom: 30px;
      font-weight: 600;
    }

    form {
      background: #ffffff;
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
      animation: fadeIn 1.5s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    label {
      font-weight: 600;
      margin-top: 10px;
      display: block;
      font-size: 0.95em;
    }

    input {
      width: 100%;
      padding: 12px;
      margin-top: 6px;
      margin-bottom: 20px;
      border: 1px solid #cce3d2;
      border-radius: 8px;
      font-size: 1em;
      outline: none;
      transition: all 0.3s ease;
    }

    input:focus {
      border-color: #4e944f;
      box-shadow: 0 0 10px rgba(78, 148, 79, 0.5);
    }

    button {
      width: 100%;
      padding: 14px;
      background-color: #4e944f;
      color: white;
      font-size: 1em;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #276749;
    }

    .ai-graphic {
      margin-top: 30px;
      text-align: center;
    }

    .ai-graphic img {
      width: 120px;
      margin-bottom: 10px;
    }

    .ai-graphic p {
      font-size: 0.9em;
      color: #3e6b47;
    }
  </style>
</head>
<body>

  <h1>LOGIN</h1>
  <form action="/login" method="POST">
    <label>Email:</label>
    <input type="email" name="email" required />

    <label>Password:</label>
    <input type="password" name="password" required />

    <button type="submit">Login</button>
  </form>

  <div class="ai-graphic">
    <img src="/IMAGES/AI.png" alt="AI Graphic" />
    <p>Powered by AI for a smarter experience!</p>
  </div>

</body>
</html>
