<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student Dashboard</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f6fff8;
      padding: 30px;
      margin: 0;
    }

    h2, h3 {
      color: #2d6a4f;
      margin-bottom: 15px;
    }

    h2 {
      font-size: 26px;
      text-align: center;
      margin-bottom: 40px;
    }

    h3 {
      font-size: 20px;
      margin-top: 30px;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      background-color: #ffffff;
      border: 1px solid #b7e4c7;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 10px;
      box-shadow: 0 2px 5px rgba(0, 128, 0, 0.1);
    }

    strong {
      color: #1b4332;
      font-size: 16px;
    }

    a {
      color: #2d6a4f;
      text-decoration: none;
      font-weight: bold;
      margin-right: 10px;
    }

    a:hover {
      text-decoration: underline;
    }

    button {
      background-color: #2d6a4f;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
    }

    button:hover {
      background-color: #40916c;
    }

    form {
      text-align: center;
      margin-top: 30px;
    }

    .center {
      text-align: center;
      margin-top: 40px;
    }

    #assignmentForm {
      display: none;
      margin-top: 20px;
    }
    .ai-banner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: linear-gradient(to right, #e6f9f0, #c2f0e0);
    border-left: 6px solid #2ecc71;
    border-radius: 12px;
    padding: 20px;
    margin: 40px auto;
    max-width: 850px;
    box-shadow: 0 4px 10px rgba(0, 100, 0, 0.1);
  }

  .ai-banner img {
    height: 100px;
    margin-left: 20px;
  }

  .ai-content {
    flex-grow: 1;
  }

  .ai-content h3 {
    font-size: 1.4rem;
    color: #1b4332;
    margin-bottom: 8px;
  }

  .ai-content p {
    font-size: 1rem;
    color: #2d6a4f;
    margin-bottom: 12px;
  }

  .ai-button {
    display: inline-block;
    background-color: #2ecc71;
    color: white;
    padding: 10px 18px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
    transition: background-color 0.3s ease;
  }

  .ai-button:hover {
    background-color: #27ae60;
  }
  </style>
</head>
<body>
  <h2>Welcome, <%= user.name %> 👨‍🎓</h2>

  <h3>📘 Notes</h3>
  <ul>
    <% notes.forEach(note => { %>
      <li>
        <strong><%= note.title %></strong><br>
        <a href="/uploads/<%= note.filename %>" target="_blank">👁️ View</a> |
        <a href="/uploads/<%= note.filename %>" download>⬇️ Download</a>
      </li>
    <% }) %>
  </ul>

  <h3>📢 Circulars</h3>
  <ul>
    <% circulars.forEach(circular => { %>
      <li>
        <strong><%= circular.title %></strong><br>
        <a href="/uploads/<%= circular.filename %>" target="_blank">👁️ View</a> |
        <a href="/uploads/<%= circular.filename %>" download>⬇️ Download</a>
      </li>
    <% }) %>
  </ul>
<div class="center">
  <a href="/upload-assignment">
  <button style="padding: 10px 20px; background-color: #077910; color: white; border: none; border-radius: 6px; cursor: pointer;">
    📤 Upload Assignment
  </button>
</a>
</div>
  <div class="center">
    <form action="/my-assignments" method="GET">
      <button type="submit">📂 My Assignments</button>
    </form>
  </div>
  <div class="ai-banner">
    <div class="ai-content">
      <h3>💡 AI-Powered Document Insights</h3>
      <p>Get instant summaries, topic breakdowns, and key questions — all generated by AI!</p>
      <a href="/ai-upload" class="ai-button">Try AI Assistant 🚀</a>
    </div>
    <img src="/images/AI.png" alt="AI Assistant">
  </div>
  
  </div>

  

  <form action="/logout" method="POST">
    <button type="submit">🚪 Logout</button>
  </form>

  <script>
  function toggleAssignmentForm() {
    const form = document.getElementById("assignmentForm");
    form.style.display = form.style.display === "none" ? "block" : "none";
  }
</script>

<% if (typeof assignmentStatus !== 'undefined' && assignmentStatus === "success") { %>
  <% if (typeof uploadSuccess !== 'undefined' && uploadSuccess) { %>
      <script>
        alert("✅ Assignment uploaded successfully!");
      </script>
    <% } else if (typeof uploadError !== 'undefined' && uploadError) { %>
      <script>
        alert("❌ Failed to upload assignment.");
      </script>
    <% } %>
<% } %>
</body>
</html>
